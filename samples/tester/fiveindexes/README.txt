Test which creates records of five different types, which with the supplied
indexerconf's can be routed to five different indexes. This is useful as a
simple test of comparing the overhead of using multiple indexes.
