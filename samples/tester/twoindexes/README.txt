Test which creates records of two different types, which with the supplied 
indexerconf's can be routed to two different indexes. This is useful as a 
simple test of comparing the overhead of using multiple indexes.
