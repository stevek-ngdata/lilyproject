package org.lilyproject.repository.api.filter;

/**
 * TODO
 *
 * <p>RecordFilters are similar to HBase's Filters, but also quite different. They don't filter
 * key-values but rather work on entire records.</p>
 */
public interface RecordFilter {
}
