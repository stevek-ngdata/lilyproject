<?xml version="1.0"?>
<blobmanager  xmlns:conf="http://kauriproject.org/configuration"
         conf:inherit="deep">

  <!--  BlobIncubatorMonitor : Monitors the BlobIncubatorTable and cleans up any unused (stale) blobs.
        minimalAge : The minimal age (in seconds) an unused blob needs to have before it gets deleted 
        monitorDelay : The minimal time (in seconds) between the checks executed by the BlobIncubatorMonitor.
                       This is used to control the load the BlobIncubatorMonitor generates on the system.  -->
  <blobIncubatorMonitor minimalAge="3600" monitorDelay="60"/>
</blobmanager>